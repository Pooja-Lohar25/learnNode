//Following code will be initiated on the start of the script automatically.
// command for automatic start of the script is "npm start" or "npm run dev"
// you can change the file to be run from the command line by changing the value  of script in package.json
// you can also use "node app.js" to start the script
// "npm start" will run the script from start attribute of scripts in package.json
// "npm run dev" will run the script from dev attribute of scripts in package.json
// "node app.js" will run the script from the command line 
//Refer to start attribute of scripts in package.json
//console.log("welcome to new landing page")

const exp = require('express')